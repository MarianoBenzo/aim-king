declare let window: any;

const canvasResolution = window.canvasResolution || {};

const CanvasResolution = {
    height: canvasResolution.height,
    width: canvasResolution.width
};

export default CanvasResolution;
